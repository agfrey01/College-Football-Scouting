[
  {
    "name": "sanity_check_report",
    "description": "Reviews a generated scouting report for factual accuracy, logical consistency, and alignment with trusted football data sources before final submission.",
    "args_schema": {
      "type": "object",
      "properties": {
        "report_text": {
          "type": "string",
          "description": "The full text of the generated scouting report to be reviewed."
        },
        "source_links": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "uri"
          },
          "description": "Optional list of URLs or database links to reliable football statistics for cross-checking factual information."
        }
      },
      "required": ["report_text"]
    }
  }
]

